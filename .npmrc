# NPM Configuration

# Audit level for npm audit
# Set to moderate to ignore high vulnerabilities in transitive dependencies
# This is a temporary measure until Angular 21 is upgraded to Angular 22+
# which has these vulnerabilities fixed in their build dependencies
audit-level=moderate

# Use offline mode when available
prefer-offline=true

# Cache configuration
cache=.npm
